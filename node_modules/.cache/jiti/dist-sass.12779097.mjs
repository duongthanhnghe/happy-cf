"use strict";Object.defineProperty(exports, "__esModule", { value: true });exports.default = void 0;var _sass = _interopRequireDefault(await jitiImport("sass"));function _interopRequireDefault(e) {return e && e.__esModule ? e : { default: e };}
const setupSass = function (customVariables) {
  const { sass, scss } = this.
  options.build.loaders;
  // Use Dart Sass
  sass.implementation = scss.implementation = _sass.default;
  // Ensure compatibility with Nuxt < 2.10 (i.e. before https://github.com/nuxt/nuxt.js/pull/6460)
  if (!sass.sassOptions) {
    delete sass.indentedSyntax;
    sass.sassOptions = {
      indentedSyntax: true
    };
  }
  // Custom variables
  if (customVariables && customVariables.length > 0) {
    const sassImports = customVariables.
    map((path) => `@import '${path}'`).
    join('\n');
    sass.additionalData = [sass.additionalData, sassImports].join('\n');
    const scssImports = customVariables.
    map((path) => `@import '${path}';`).
    join('\n');
    scss.additionalData = [scss.additionalData, scssImports].join('\n');
  }
};var _default = exports.default =
setupSass; /* v9-490b72c903bf3f8c */
